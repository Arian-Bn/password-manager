# Password Manager

C++20 password manager with client-server architecture.

## Build Instructions

```bash
mkdir build && cd build
cmake .. -DCMAKE_BUILD_TYPE=Release
cmake --build . --parallel
./password_manager
